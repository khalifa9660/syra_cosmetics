/**
 * Overlay de grain cinématique — texture fixe appliquée sur tout le site.
 * Utilise un SVG fractal noise encodé en data URI.
 */
export function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />;
}
